<shortcuts xmlns:android="http://schemas.android.com/apk/res/android">
    <shortcut
        android:shortcutId="StartProxyShortCut"
        android:enabled="true"
        android:icon="@drawable/ic_baseline_vpn_key_48"
        android:shortcutShortLabel="@string/switch_shortcut_label"
        android:shortcutLongLabel="@string/switch_shortcut_long_label"
        android:shortcutDisabledMessage="@string/switch_shortcut_disabled_msg">
        <intent
            android:action="android.intent.action.VIEW"
            android:targetPackage="com.github.kr328.clash.foss"
            android:targetClass="com.github.kr328.clash.ScSwitchProxyActivity" />
        <!-- If your shortcut is associated with multiple intents, include them
             here. The last intent in the list determines what the user sees when
             they launch this shortcut. -->
    </shortcut>
    <!-- Specify more shortcuts here. -->
</shortcuts>